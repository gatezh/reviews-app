export default function Header(): React.ReactElement {
  const styles = {
    padding: '0 1rem',
    backgroundColor: 'black',
    color: 'white',
    lineHeight: '3rem',
    // textAlign: 'left'
  }

  return <header style={styles}>⭐️ Latest reviews</header>;
}
